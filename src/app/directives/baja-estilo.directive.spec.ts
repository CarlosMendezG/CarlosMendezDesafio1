import { BajaEstiloDirective } from './baja-estilo.directive';

describe('BajaEstiloDirective', () => {
  it('should create an instance', () => {
    const directive = new BajaEstiloDirective();
    expect(directive).toBeTruthy();
  });
});
